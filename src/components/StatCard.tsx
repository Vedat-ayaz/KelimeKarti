import { StyleSheet, Text, View } from 'react-native';
import { useAppColors } from '../theme/colors';
export function StatCard({ label, value, accent = false }: { label: string; value: number; accent?: boolean }) { const c = useAppColors(); return <View style={[styles.box, { backgroundColor: accent ? c.primarySoft : c.surface, borderColor: accent ? c.primarySoft : c.border }]}><Text style={[styles.value, { color: accent ? c.primary : c.text }]}>{value}</Text><Text numberOfLines={1} style={[styles.label, { color: accent ? c.primary : c.muted }]}>{label}</Text></View>; }
const styles = StyleSheet.create({ box: { flex: 1, borderWidth: StyleSheet.hairlineWidth, borderRadius: 18, paddingHorizontal: 14, paddingVertical: 15 }, value: { fontSize: 25, fontWeight: '700', letterSpacing: -0.7 }, label: { fontSize: 12, marginTop: 5, fontWeight: '500' } });
