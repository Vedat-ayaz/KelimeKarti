import { Ionicons } from '@expo/vector-icons';
import { Pressable, StyleSheet, Text, View } from 'react-native';
import { Word } from '../domain/entities/Word';
import { useAppColors } from '../theme/colors';
import { AppCard } from './AppCard';
import { LearnedBadge } from './LearnedBadge';
export function WordListItem({ word, onEdit, onDelete, onToggle }: { word: Word; onEdit(): void; onDelete(): void; onToggle(): void }) { const c = useAppColors(); return <AppCard><View style={styles.row}><Pressable onPress={onToggle} hitSlop={8} style={[styles.status, { backgroundColor: word.isLearned ? c.successSoft : c.surfaceMuted }]}><Ionicons name={word.isLearned ? 'checkmark' : 'ellipse-outline'} size={18} color={word.isLearned ? c.success : c.muted} /></Pressable><View style={styles.content}><Text numberOfLines={2} style={[styles.term, { color: c.text }]}>{word.term}</Text><Text numberOfLines={2} style={[styles.translation, { color: c.muted }]}>{word.translation}</Text><LearnedBadge learned={word.isLearned} onPress={onToggle} /></View><View style={styles.actions}><Pressable accessibilityLabel="Düzenle" onPress={onEdit} hitSlop={10} style={[styles.iconButton, { backgroundColor: c.surfaceMuted }]}><Ionicons name="pencil-outline" size={17} color={c.text} /></Pressable><Pressable accessibilityLabel="Sil" onPress={onDelete} hitSlop={10} style={styles.iconButton}><Ionicons name="trash-outline" size={18} color={c.danger} /></Pressable></View></View></AppCard>; }
const styles = StyleSheet.create({ row: { flexDirection: 'row', gap: 12, alignItems: 'flex-start' }, status: { width: 34, height: 34, borderRadius: 12, alignItems: 'center', justifyContent: 'center' }, content: { flex: 1, alignItems: 'flex-start', gap: 7 }, actions: { gap: 7, alignItems: 'center' }, term: { fontSize: 18, fontWeight: '700', letterSpacing: -0.25 }, translation: { fontSize: 14, lineHeight: 20 }, iconButton: { width: 34, height: 34, borderRadius: 11, alignItems: 'center', justifyContent: 'center' } });
