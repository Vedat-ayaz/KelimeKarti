import { StyleSheet, Text, TextInput, TextInputProps, View } from 'react-native';
import { useAppColors } from '../theme/colors';
export function AppTextInput({ label, error, ...props }: TextInputProps & { label: string; error?: string }) { const c = useAppColors(); return <View style={styles.group}><Text style={[styles.label, { color: c.text }]}>{label}</Text><TextInput placeholderTextColor={c.muted} {...props} style={[styles.input, { color: c.text, backgroundColor: c.surface, borderColor: error ? c.danger : c.border }, props.multiline && styles.multiline]} />{error ? <Text style={{ color: c.danger }}>{error}</Text> : null}</View>; }
const styles = StyleSheet.create({ group: { gap: 7 }, label: { fontSize: 14, fontWeight: '600' }, input: { minHeight: 50, borderWidth: 1, borderRadius: 12, paddingHorizontal: 14, fontSize: 16 }, multiline: { minHeight: 92, paddingTop: 14, textAlignVertical: 'top' } });
